{
  "name": "@example/basics",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "wrangler pages dev ./dist",
    "astro": "astro",
    "deploy": "wrangler pages publish dist",
    "deploy:login": "wrangler login"
  },
  "dependencies": {
    "astro": "^1.1.5"
  },
  "devDependencies": {
    "wrangler": "^2.0.28"
  }
}
